# requirements.txt

Flask
requests
# Add any other libraries you might need, e.g.:
# python-dotenv  # If you use a .env file for secrets
# gunicorn       # Optional: For production serving within Replit (though Replit often handles this)
# Add database driver if needed (e.g., psycopg2-binary, SQLAlchemy, etc.)